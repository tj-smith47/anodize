## Archived 2026-04-16 (from /opt/repos/anodize/.claude/known-bugs.md)

- [x] 2026-04-16 parity-audit: Git snapshot-mode short-circuit — `detect_git_info` now returns a synthetic `GitInfo` (empty commit/branch/etc., zero semver) when the working dir is not inside a git repo, letting `anodize release --snapshot` run from a fresh tarball or scratch directory. `core/src/git.rs:240-280`.
- [x] 2026-04-16 fitness-audit: `nfpm` deb `arch_variant` — kept the **config field** (so users running `RUSTFLAGS="-C target-cpu=x86-64-v3"` builds can manually tag the resulting deb) but reverted the **auto-derive from artifact metadata** since the build stage doesn't populate `goamd64` on Rust binaries — the auto-derive was dead plumbing copied from GoReleaser's Go-toolchain conventions. `core/src/config.rs:2995-3017`, `stage-nfpm/src/lib.rs:222-242,702-737`. The broader question of whether anodize should auto-detect microarch from RUSTFLAGS is captured under "Rust-appropriateness flags".
- [x] 2026-04-16 verified-correct: `--skip=before` honored at `cli/src/commands/release/mod.rs:306` (`ctx.should_skip("before")`).
- [x] 2026-04-16 verified-correct: Before-hook ordering — git context resolves at `release/mod.rs:298`, before-hooks run at `:304-317`, so `{{ .Tag }}` etc. are populated when hooks fire.
- [x] 2026-04-16 verified-correct: `project_name` Cargo.toml inference — already extracted to `helpers::infer_project_name` and called from release/build/check/continue.
- [x] 2026-04-16 verified-correct: Nightly `name_template` default — `release/mod.rs:389` uses `"{{ ProjectName }}-nightly"` when nightly_cfg.name_template is unset.
- [x] 2026-04-16 verified-correct: Dead skip warn-loop in `pipeline.rs` — already removed; `pipeline.rs:604-607` has only a comment noting validation runs at the CLI entry.
- [x] 2026-04-16 fitness-audit: `config.env` map vs list — kept BOTH forms intentionally. The map form (`FOO: bar`) is the natural Rust YAML idiom; deprecating it because GoReleaser only accepts list form would worsen DX for Rust-native users with no benefit. Drop-in GoReleaser configs continue to work via the list form. Removed from active findings.
- [x] 2026-04-15 parity-audit: P2/P10 Custom publisher default filter — `matches_publisher_filter` narrowed to GoReleaser's curated `filterArtifacts` set (`exec.go:128-159`): Archive / UploadableBinary / UploadableFile / LinuxPackage / DockerImage / DockerManifest / DockerImageV2 / SourceArchive / Sbom / PySdist / PyWheel only, with Checksum / Metadata / Signature / Certificate as explicit opt-ins. Raw Binary and other kinds now require `artifact_types`. `cli/src/commands/publisher.rs:280-345`.
- [x] 2026-04-15 parity-audit: Archive / Source / Checksum / UB false-positive sweep — verified fixed: B16 (refresh_combined_checksums + call from release stage), B1 (binary_info mode defaults to "0755"), E16 (strip_parent+dst expanded per-file at `stage-archive/src/lib.rs:505-526`), B34 (UB source-binary Extras propagated at `stage-build/src/lib.rs:529-540`), B10 (CSV metadata exposed as array via `refresh_artifacts_var` CSV_LIST_KEYS at `core/src/context.rs:596-617`), B27 (source archive preserves full relative paths at `stage-source/src/lib.rs:217-234`), B12/B13 (archive propagates replaces + dynamically_linked at `stage-archive/src/lib.rs:1526-1560`), C5 (UB ids default to `[id]` at `stage-build/src/lib.rs:369-374`), D11 (UniversalBinaryConfig already has `id: Option<String>` at `core/src/config.rs:854-869`), B20 (split checksum uses `ChecksumOf` key at `stage-checksum/src/lib.rs:517-520`).
- [x] 2026-04-15 parity-audit: D1/D2/D3 Docker ID uniqueness — now hard-errors on duplicate `id` across `docker[]` and `docker_v2[]` config blocks per crate, matching GoReleaser `internal/ids/ids.go:26-36` + `docker.go:111` / `v2/docker.go:93`. Previously V2 only warned and V1 was unchecked. `stage-docker/src/lib.rs:1545-1578`.
- [x] 2026-04-15 parity-audit: D4 Docker V2 retry scope — split the retry predicate: V1 keeps the broad network/HTTP allowlist (unchanged), V2 is narrowed to `"manifest verification failed for digest"` only, matching GoReleaser `v2/docker.go:544-549` `isRetriableManifestCreate`. `stage-docker/src/lib.rs:91-131,880-885`.
- [x] 2026-04-15 parity-audit: D11 Legacy Docker cardinality check — `docker.ids: [a,b,c]` now errors when the matched binaries don't cover N distinct IDs, matching GoReleaser `docker.go:170-172`. `stage-docker/src/lib.rs:1665-1691`.
- [x] 2026-04-15 parity-audit: D14 `docker manifest rm` — all errors now ignored, matching GoReleaser `internal/pipe/docker/api_docker.go:26`. Previously bailed on any stderr that wasn't "no such manifest" / "not found". `stage-docker/src/lib.rs:2511-2524`.
- [x] 2026-04-15 parity-audit: D15 `docker_digest.name_template` — verified fixed; `stage-docker/src/lib.rs:2798-2809` resolves the first non-empty template across crates and falls back to `digests.txt`.
- [x] 2026-04-15 parity-audit: K2 Krew one-binary-per-archive — verified fixed; `stage-publish/src/krew.rs:331-338` bails when `archive.extra_binaries().len() != 1`.
- [x] 2026-04-15 parity-audit: B-48 SBOM binary-like filter — `artifacts: binary` now matches Binary + UploadableBinary + UniversalBinary with path-dedup preferring UploadableBinary, mirroring GoReleaser `artifact.ByBinaryLikeArtifacts` (`internal/artifact/artifact.go:733-761`). Previously only matched `Binary`, missing UniversalBinary SBOM coverage and skipping UploadableBinary promotions. `stage-sbom/src/lib.rs:397-475`.
- [x] 2026-04-15 parity-audit: F-1 Publisher order — verified correct; `stage-publish/src/lib.rs:93-169` runs crates.io → infra (dockerhub/artifactory/fury/cloudsmith/npm/upload) → package managers (homebrew/scoop/chocolatey/winget/aur/krew/nix/casks) → aur_source, matching GoReleaser `publish.go:46-74`.
- [x] 2026-04-15 parity-audit: B-41 GitHub release PATCH draft clobber — verified fixed; `stage-release/src/lib.rs:1991` always creates as draft; `:2028-2038` preserves existing.draft when PATCHing existing_by_tag; `:2342-2368` un-drafts at publish when `user_wants_draft=false`. Matches GoReleaser `github.go:541` end-to-end.
- [x] 2026-04-15 parity-audit: B-15 Sign subprocess env vars — verified fixed; `stage-sign/src/lib.rs:722-754` merges shell_vars (artifact/signature/certificate/artifactName/artifactID/digest) into SignJob.env, which `execute_sign_job` applies via `command.envs()` at `:295-296`.
- [x] 2026-04-15 parity-audit: B-24 Changelog git-backend SHA abbrev — verified fixed; `stage-changelog/src/lib.rs:500` `vars.set("SHA", &short_sha)` already respects abbrev.
- [x] 2026-04-15 parity-audit: B-36 Release extra_files silent skip — verified fixed; `stage-release/src/lib.rs:496,523` bail! on zero matches.
- [x] 2026-04-15 parity-audit: C-2 SBOM multi-document validation — verified fixed; `stage-sbom/src/lib.rs:360-366` bails when `artifacts != "any"` and `documents.len() > 1`.
- [x] 2026-04-15 parity-audit: B-1/B-2 Sign `artifacts: all` scope now delegates to `anodize_core::artifact::release_uploadable_kinds()` (matches GoReleaser `sign.go:103-104` using `ReleaseUploadableTypes`). Dropped Binary/UniversalBinary/Snap/Installer/DiskImage/MacOsPackage/Header/CArchive/CShared from the "all" match — users must opt in via the dedicated filter values. `stage-sign/src/lib.rs:71-86`. Tests at 1320-1367, 1525-1560, 3428-3474 updated.
- [x] 2026-04-15 parity-audit: B-3/B-4 Release `include_meta` — flag is now wired: when `include_meta: true`, `ArtifactKind::Metadata` is appended to the upload list (matches GoReleaser `release.go:160-163`). Removed dead ids-filter allowlist arm that referenced Metadata, and removed the redundant sig_cert_entries block (sign stage already registers Signature/Certificate as first-class kinds, not as Metadata artifacts). `stage-release/src/lib.rs:1124-1180`.
- [x] 2026-04-15 parity-audit: B-22/B-23 Milestone provider resolution — close_milestones now uses `ctx.token_type` to pick between GitHub/GitLab/Gitea APIs, and `resolve_milestone_repo` prefers the crate's matching-provider release block before any other. Mixed-provider configs no longer misroute milestone closes. `cli/src/commands/release/milestones.rs:38-94,115-170`. `resolve_milestone_provider` deleted as redundant.
- [x] 2026-04-15 parity-audit: A11 AUR push branch — hardcoded to `Some("master")` so pushes always target AUR's mandated branch regardless of git's local clone default. `stage-publish/src/aur.rs:551-564`.
- [x] 2026-04-15 user-report: Announce emails rendering `{{ ReleaseURL }}` as `https://github.com/<owner>/<repo>/releases/tag/untagged-<sha>` (404 after publish). Cause: anodize captured `html_url` from the `POST /releases` response, which GitHub returns as `untagged-<sha>` for draft releases; the publish-time un-draft PATCH later updated the real URL but anodize was holding the stale draft URL. Fix: construct the URL deterministically from `{gh_download_base}/{owner}/{name}/releases/tag/{tag}` (matches GoReleaser `internal/pipe/release/scm.go:26-33`). `stage-release/src/lib.rs:2077-2094`. GitLab/Gitea paths already constructed URLs deterministically — only GitHub was affected.
- [x] 2026-04-15 parity-audit: GitLab JOB-TOKEN safety — anodize now only sends `JOB-TOKEN` when all three hold: `CI_JOB_TOKEN` env is non-empty, `gitlab_urls.use_job_token` is true, AND the effective token equals `CI_JOB_TOKEN`. Matches GoReleaser `internal/client/gitlab.go:642` `checkUseJobToken`. Helper `resolve_use_job_token` in `stage-release/src/gitlab.rs`; wired at `stage-release/src/lib.rs:1458` and inlined at `stage-changelog/src/lib.rs:1086`. 5 new unit tests mirroring GoReleaser's `TestGitLabCheckUseJobToken` table.
- [x] 2026-04-15 parity-audit: B-5 binary_signs default template had stray `.sig` suffix diverging from GoReleaser `sign_binary.go:16`. Fixed: removed suffix; tests updated. `stage-sign/src/lib.rs:24`.
- [x] 2026-04-15 parity-audit: C5 Chocolatey template renders missing `Changelog` variable (GoReleaser `WithExtraFields`). Fixed: inject `Changelog` from `ReleaseNotes` in `stage-publish/src/chocolatey.rs:457-475`.
- [x] 2026-04-15 parity-audit: W7 Winget template renders missing `Changelog` variable. Fixed: same pattern applied in `stage-publish/src/winget.rs:779-792`.
- [x] 2026-04-15 parity-audit: W8 Winget `release_date` not coerced to YYYY-MM-DD. Fixed: slice first 10 chars of RFC-3339 Date; format-validated. `stage-publish/src/winget.rs:776`.
- [x] 2026-04-15 parity-audit: S4 Scoop bin paths used `\` separator; GoReleaser `scoop.go:384` uses `filepath.ToSlash`. Fixed: forward-slash. `stage-publish/src/scoop.rs:117`.
- [x] 2026-04-15 parity-audit: D5 Archive default ID missing — `archive.id` was Optional, downstream `ids:` filters couldn't match. Fixed: default to `"default"` when unset; always emit in metadata. `stage-archive/src/lib.rs:1471-1481`.
- [x] 2026-04-15 parity-audit: `MonorepoConfig` / `WorkspaceConfig` lacked `deny_unknown_fields` (typos silently accepted). Fixed: added to both structs. `core/src/config.rs:5108,5175`.

## Archived 2026-04-16 (from /opt/repos/anodize/.claude/known-bugs.md)

- [x] 2026-04-16 parity-audit: **`tag_pre_hooks` / `tag_post_hooks` wired on the `tag` subcommand.** Added `tag_pre_hooks` and `tag_post_hooks: Option<Vec<HookEntry>>` to `TagConfig`; the `create_tag` closure in `commands/tag.rs` renders a minimal `TemplateVars` context (`Tag`, `PrefixedTag`, `Version`, `PreviousTag`), exports `ANODIZE_CURRENT_TAG` / `ANODIZE_PREVIOUS_TAG` into the process env, and invokes both hook lists via `anodize_core::hooks::run_hooks` (dry-run honoured). Also removed the stale `// TODO: Wire GitConfig (ignore_tags, ignore_tag_prefixes) into tag discovery` by extending `get_all_semver_tags` / `get_branch_semver_tags` to accept `Option<&GitConfig>` + `Option<&TemplateVars>` and applying the `ignore_tags` (glob) + `ignore_tag_prefixes` (starts_with) filters; callers in `find_previous_tag` now load `config.git` and pass it through. 5 new tests (2 YAML roundtrip on TagConfig, 3 git ignore_tags / ignore_tag_prefixes integration).
- [x] 2026-04-16 hook-infra: **Lazy-write-off guard added** — `.claude/hooks/guard-bugs-closure.sh` (PreToolUse on Edit/Write). Detects `[ ]`→`[x]` closures in this file and blocks unless (a) session diff touched a path or backticked identifier named in the entry, (b) the entry contains an `audit: <path-or-identifier>` line the hook can resolve (grep against crates/ or filesystem existence), or (c) the entry contains an in-band `AUDITED: <reason>` override (reviewable in git blame). No env-var bypass — every skip must leave a git-blameable trail. Manual test cases pass (session-overlap allow, fake-citation block, real-citation allow, AUDITED override, naked-closure block). Prevents the grep-only "already done" write-off that almost slipped through the `nfpm`→`nfpms` detector this turn.
- [x] 2026-04-16 parity-audit: **`ctx.deprecate()` fully wired**. `detect_deprecated_aliases` in `cli/src/pipeline.rs` now catches: removed publishers (`gemfury`/`fury`/`npms`), `snapshot.name_template`→`version_template`, `announce.email.body_template`→`message_template`, `archives[].format`→`formats`, `archives[].format_overrides[].format`→`formats`, `nfpm[].builds`→`ids` (was silent: previous code checked `nfpms` but the field is `nfpm` — dead check), `nfpm[].maintainer` missing, `snapcrafts[].builds`→`ids`, per-crate publisher `goamd64`/`goarm`→`amd64_variant`/`arm_variant` for homebrew/scoop/chocolatey/winget/aur/aur_source/nix, and top-level `homebrew_casks[].goamd64`. 15 unit tests including regression guard for the nfpm field-name bug. Surfaced at config load by `build.rs`, `check.rs`, `release/mod.rs`, `continue_cmd.rs`.
- [x] 2026-04-16 fitness-audit: **Microarch rename + RUSTFLAGS wiring** — `goamd64` → `amd64_variant`, `goarm` → `arm_variant` across config (7 publisher structs with `#[serde(alias = ...)]` for compat), util.rs OsArtifact + filter functions (`filter_by_variant`, `find_*_with_variant`), and all 7 publisher modules (~70 sites). New `parse_amd64_variant_from_rustflags` in `stage-build/src/lib.rs` detects `-C target-cpu=x86-64-vN` (compact and spaced forms) and tags Binary artifacts with `metadata["amd64_variant"]`. nfpm deb auto-derive re-enabled — copies artifact `amd64_variant` into `deb.arch_variant` when unset. ARM target-triple wiring deferred (Rust triples already encode ARM version). Tests: 8 new unit tests for the parser; suite at 3108/0.
- [x] 2026-04-16 fitness-audit: **npm publisher removed** — `stage-publish/src/npm.rs` deleted, `NpmConfig` struct deleted, module refs removed from `stage-publish/src/lib.rs`, `Config.npms` field removed. NPM targets JavaScript packages; no first-class Rust use case. Users porting GoReleaser configs get a deprecation warning via `detect_deprecated_aliases`.
- [x] 2026-04-16 fitness-audit: **GemFury (`fury`) publisher removed** — `stage-publish/src/fury.rs` deleted, `FuryConfig` struct deleted, `gemfury` alias removed. Users wanting deb/rpm hosting use `cloudsmiths:` or `artifactories:`. Deprecation warning emitted for both `fury:` and `gemfury:` config keys.
- [x] 2026-04-16 fitness-audit: **PyWheel/PySdist artifact kinds removed** — no producer wired (no maturin integration), no consumer that would fail gracefully. Removed from `ArtifactKind` enum, `as_str`/`parse` mappings, and all allowlists (`size_reportable_kinds`, `uploadable_kinds`, `release_uploadable_kinds`, artifactory default list, custom publisher default list, docker V2 stageable list, sign `all` tests). Roundtrip test count updated 44→42.
- [x] 2026-04-16 parity-audit: B27 Makeself filename — default template now appends Arm/Mips/Amd64-non-v1 variant suffixes from the per-target template vars. Multi-target ARM/MIPS builds for the same project no longer collide on disk. `stage-makeself/src/lib.rs:288-336`.
- [x] 2026-04-16 parity-audit: Git snapshot-mode short-circuit — `detect_git_info` returns synthetic empty `GitInfo` when not in a git repo, so `anodize release --snapshot` works from a fresh tarball. `core/src/git.rs:240-280`.
- [x] 2026-04-16 fitness-audit: `nfpm` deb `arch_variant` config field added; auto-derive re-enabled later same day after RUSTFLAGS-based amd64_variant producer wired in stage-build (see Microarch entry above). Users can either set `arch_variant` explicitly or let it auto-derive from artifact metadata. `core/src/config.rs:2995-3017`, `stage-nfpm/src/lib.rs`.
- [x] 2026-04-16 verified-correct sweep: `--skip=before` guard, before-hook ordering, `project_name` extraction, Nightly default, dead skip warn-loop, B18 `package_name` default, C4/C9 `builds`→`ids` aliases — all already done.
- [x] 2026-04-16 fitness-audit: `config.env` map vs list — kept BOTH forms; the map form is the natural Rust YAML idiom and deprecating it would worsen DX with no benefit.

## Archived 2026-04-17 (from /opt/repos/anodize/.claude/known-bugs.md)

- [x] 2026-04-16 inventory (A1-rev): `nfpm.if` Pro field absent from `NfpmConfig` — schema rejects user configs copied from GoReleaser docs. FIXED 2026-04-16: added `if_condition: Option<String>` with `#[serde(rename = "if")]` to `NfpmConfig` (core/src/config.rs:2891) and wired skip-on-falsy at `stage-nfpm/src/lib.rs:973` using hard-error on template-render failure (avoids W1 silent-skip footgun). 4 new unit tests (false / empty-render / truthy / render-error).
- [x] 2026-04-16 inventory (A1-rev): `nfpm.templated_contents` + `nfpm.templated_scripts` Pro fields absent from `NfpmConfig` — silent serde drop. FIXED 2026-04-16: both fields added to `NfpmConfig` (core/src/config.rs); `stage-nfpm/src/lib.rs` reads each src file, renders body through Tera, writes to `dist/nfpm-tmp/<crate>/<nfpm_id>/`, and substitutes the temp path into the emitted nFPM YAML (`templated_scripts` wins over same-named `scripts` entry). 2 new unit tests (contents body interpolation + script body interpolation).
- [x] 2026-04-16 inventory (A1-rev): `archives[].hooks` field-name mismatch — anodize uses `pre`/`post`, GoReleaser docs use `before`/`after`. Config parses successfully but hooks never run (silent skip). Add serde aliases AND ensure both names execute. — resolved 2026-04-16. `#[serde(alias = "before")]` added to `BuildHooksConfig.pre` and `#[serde(alias = "after")]` to `.post` (core/src/config.rs:924). Both spellings now land on the same fields → same execution path, no silent skip. Regression-guard test `test_config_parse_archive_hooks_goreleaser_spelling` added in stage-archive. /verify PASS: 3181 tests, 0 failures; clippy clean.
- [x] 2026-04-16 inventory (A1-rev): `release.header.from_url` + `release.footer.from_url` need `headers`/`auth` map and template-rendered URL/path. Currently naked `String` with no auth, no template render, no retry, single-shot 30s timeout. — resolved 2026-04-17. `ContentSource::FromUrl` now carries `headers: Option<HashMap<String, String>>` (core/src/config.rs:1314). `resolve_content_source(source, ctx)` takes `&Context` and template-renders: `from_file` path, `from_url` URL, and every header VALUE (keys literal per GoReleaser docs). Added 3-try exponential backoff (500ms/1s/2s) on transient network errors + 5xx; 4xx bails fast without retry. 3 new tests: `from_file_path_is_template_rendered`, `from_url_with_headers_parses`, `from_url_without_headers_parses` (backwards-compat). /verify PASS: 3184 tests, 0 failed, clippy clean. Also addresses A5 W2 (naked timeout/no retry) and A5 W8 (from_file template-render) from pro-features-audit.md.
- [x] 2026-04-16 inventory (A1-rev): `metadata.full_description` (with `from_file`/`from_url`) + `metadata.commit_author` Pro fields absent from `MetadataConfig`. — resolved 2026-04-17. `full_description: Option<ContentSource>` and `commit_author: Option<CommitAuthorConfig>` (reusing the existing shared `CommitAuthorConfig` with name/email/signing) added to `MetadataConfig` (core/src/config.rs:4422-4428). `populate_metadata_var` now fallible: resolves `FullDescription` from Inline + FromFile (template-rendered path) in-core; FromUrl returns an actionable error rather than silent-skip (W1 lesson — tracked as limitation for a future fix that either moves URL resolution into a late-pipeline stage or adds reqwest to core). `CommitAuthor.{Name,Email}` exposed as nested template vars. 4 new tests: inline / from_file / from_url-errors / commit_author. /verify PASS: 3188 tests (+4), clippy clean.
- [x] 2026-04-16 inventory (A1-rev): `metadata.{homepage,license,description,maintainers}` collected but never consumed by any publisher — config wires to nothing (`feedback_config_must_wire.md` violation). — resolved 2026-04-17. Added 5 Config helpers (`meta_homepage` / `meta_license` / `meta_description` / `meta_maintainers` / `meta_first_maintainer`) and wired fallback in the two `required`-tier publishers: `stage-nfpm` (homepage/license/description/maintainer — all 4 GoReleaser fields) and `stage-publish/homebrew` (homepage/description/license). Publishers default from metadata when their own field is unset; override still works. 2 new tests confirm fallback+override. /verify PASS: 3190 tests, clippy clean.
- [x] 2026-04-17 followup (metadata-must-wire): extend `metadata.*` fallback to remaining publishers where GoReleaser Pro defaults them: scoop (homepage/description/license), chocolatey (description/license/authors), winget (license/description/homepage), nix (description/license/homepage), snap (description), krew (description), aur (license+homepage+maintainers). — resolved 2026-04-17. All 7 publishers now fall back through `ctx.config.meta_*()` helpers when their own field is unset. Snapcraft upstream has no homepage/license fields, so only description was wired. /verify PASS: 3190 tests, clippy clean (one post-edit clippy cleanup: `iter().cloned().collect()` → `.to_vec()`).
- [x] 2026-04-16 inventory (A1-rev): `dmgs[].if` Pro field absent from `DmgConfig`. — resolved 2026-04-17. Added `if_condition: Option<String>` with `#[serde(rename = "if")]` to `DmgConfig` and wired skip-on-falsy in `stage-dmg` with hard-error on render failure (W1 footgun avoidance). 3 new unit tests (false / empty-render / render-error). /verify PASS: 3193 tests, clippy clean.
- [x] 2026-04-16 inventory (A1-rev): `msis[].if` + `goamd64` + `before`/`after` hooks absent from `MsiConfig`. (`extra_files` confirmed present at config.rs:3443.) — resolved 2026-04-17. Added `if_condition: Option<String>` (`#[serde(rename = "if")]`) and `hooks: Option<BuildHooksConfig>` to `MsiConfig`. Wired pre-msi / post-msi hook runs in `stage-msi` bracketing the per-target build loop; hard-error on render failure (W1 avoidance). `goamd64` intentionally NOT added: Go's `amd64v1/v2/v3/v4` variant selector has no direct Rust analogue (Rust uses `-C target-cpu=...` instead), so it stays not-applicable for the Rust ecosystem. 3 new tests (if=false, render-failure-bails, before/after yaml aliases parse correctly). /verify PASS: 3196 tests, clippy clean.
- [x] 2026-04-16 inventory (A1-rev): `pkgs[].if` Pro field absent from `PkgConfig`. — resolved 2026-04-17. Added `if_condition: Option<String>` with `#[serde(rename = "if")]` to `PkgConfig` and wired skip-on-falsy in `stage-pkg` with hard-error on render failure (W1 avoidance). 2 new tests. /verify PASS: 3198 tests, clippy clean.
- [x] 2026-04-16 inventory (A1-rev): `nsis[].if` + `goamd64` Pro fields absent from `NsisConfig`. — resolved 2026-04-17. Added `if_condition: Option<String>` (`#[serde(rename = "if")]`) to `NsisConfig` and wired skip-on-falsy in `stage-nsis` with hard-error on render failure (W1 avoidance). `goamd64` intentionally NOT added — same rationale as MSI: Go's amd64v1/v2/v3/v4 variant selector has no direct Rust analogue, stays not-applicable. 2 new tests. /verify PASS: 3200 tests, clippy clean.
- [x] 2026-04-16 inventory (A1-rev): `app_bundles[].if` Pro field absent from `AppBundleConfig`. — resolved 2026-04-17. Added `if_condition: Option<String>` (`#[serde(rename = "if")]`) to `AppBundleConfig` and wired skip-on-falsy in `stage-appbundle` with hard-error on render failure (W1 avoidance). 2 new tests. /verify PASS: 3202 tests, clippy clean.
- [x] 2026-04-16 inventory (A1-rev): `--prepare` CLI flag absent — zero hits in `crates/cli/src/`. — resolved 2026-04-17. Added `--prepare` flag to `Commands::Release` clap variant + `prepare: bool` field to `ReleaseOpts`. Wired via an extracted idempotent helper `apply_prepare_mode_to_skip()` that augments the skip list with `release`/`publish`/`announce` at the top of `run()`. Semantic exactly matches `--skip=release,publish,announce` (build + archive + sign + checksum + sbom all still run, artifacts land in `dist/`). 4 new tests: clap parse, helper from empty, helper preserves user skips, helper is idempotent. /verify PASS: 3206 tests, clippy clean.
- [x] 2026-04-16 parity-build-archive (BLOCKER): Windows `format: binary` archives miss `.exe` suffix. — resolved 2026-04-17. In `stage-archive`, the binary-format branch now calls `anodize_core::target::is_windows(target)` and appends `.exe` when the target is Windows (and the stem doesn't already end with it). Matches upstream archive.go:298-302. 2 new regression tests (Windows gets .exe, Linux/macOS keeps no extension). /verify PASS: 3208 tests, clippy clean.
- [x] 2026-04-16 parity-build-archive (BLOCKER): `meta: true` archive with zero matching files emits empty archive silently — missing guard vs GoReleaser archive.go:217. — resolved 2026-04-17. In `stage-archive`, after `all_entries` is assembled, added `if is_meta && all_entries.is_empty() { bail! ... }` with an actionable error naming the crate/target and pointing the user at their `files:` patterns. 1 new regression test. /verify PASS: 3209 tests, clippy clean.
- [x] 2026-04-16 parity-build-archive (BLOCKER): `universal_binaries.name_template` default is source binary filename instead of `{{ .ProjectName }}` (vs upstream universalbinary.go:45). — resolved 2026-04-17. In `stage-build::build_universal_binary`, the default branch now renders `{{ .ProjectName }}` explicitly (kept `binary_name` for the `binary` metadata key). Added regression test `test_universal_binary_default_name_uses_project_name` that registers source binaries as `myapp-bin` with project_name `myapp` and asserts the universal filename is `myapp`. /verify PASS: 3212 tests, clippy clean.
- [x] 2026-04-16 parity-build-archive (WARN): binary-format plural detection edge case (see audit file). — resolved 2026-04-17. In `stage-archive`, the allow_different_binary_count exemption now treats `format == "binary"` OR `formats` containing `"binary"` as the binary format (previously only singular `format` was checked). Added regression test `test_binary_format_plural_exempts_different_binary_count_check`. /verify PASS: 3213 tests, clippy clean.
- [x] 2026-04-16 parity-build-archive (WARN): `Algorithm` template var unavailable in extra_files name_template path. — resolved 2026-04-17. In `stage-checksum`, added `vars.set("Algorithm", &algorithm)` to the combined-checksum alias render path so `extra_files[].name_template` can use `{{ .Algorithm }}` just like the sidecar path. Added regression `test_extra_files_name_template_exposes_algorithm_var`. /verify PASS: 3214 tests, clippy clean.
- [x] 2026-04-16 parity-build-archive (WARN): filename-containing-doublespace sort edge case (inherited from GoReleaser; flag for divergence test). — resolved 2026-04-17. In `stage-checksum` combined-line sort, documented the inherited GoReleaser behavior in-source and added `test_combined_sort_doublespace_divergence` to pin the sort-key shape. If someone ever "fixes" the sort without intent, the test breaks and surfaces the divergence. /verify PASS: 3215 tests.
- [x] 2026-04-16 parity-build-archive (SUGGEST): add `#[serde(alias = "builds")]` on `ArchiveConfig.ids`. — resolved 2026-04-17. Added `#[serde(alias = "builds")]` to `ArchiveConfig.ids` (consistent with `NfpmConfig.ids` and `DockerManifestsConfig.image_templates`). Added regression `test_parse_archives_accepts_deprecated_builds_alias`. /verify PASS: 3216 tests.
- [x] 2026-04-16 parity-build-archive (SUGGEST): mark auto-included LICENSE/README files with `Default: true` for diagnostics. — resolved 2026-04-17. Added `default: bool` to `ResolvedExtraFile`; `resolve_default_extra_files` sets `default: true` while user-configured specs set `default: false`. Mirrors GoReleaser's `Default: true` flag on archive files. Added regression `test_resolve_default_extra_files_marks_default_flag` (serialized on cwd). /verify PASS: 3217 tests, clippy clean.
- [x] 2026-04-16 parity-publishers (WARN): stage-release hardcodes 20-kind upload list — over-uploads Snap+Pro types on GitHub. Use `release_uploadable_kinds()` helper. — resolved 2026-04-17. Replaced hardcoded 20-kind `upload_kinds` Vec with `anodize_core::artifact::release_uploadable_kinds().to_vec()`, preserving the `IncludeMeta` conditional append. Drops Snap/DiskImage/Installer/MacOsPackage/UniversalBinary/CArchive/CShared/Header from the GitHub-release upload set to match GoReleaser's `ReleaseUploadableTypes()`. /verify PASS: 3217 tests, clippy clean.
- [x] 2026-04-16 parity-publishers (WARN): Docker v1 `stage_binaries` only stages `Binary`; misses `LinuxPackage`/`CArchive`/`CShared` (v2 path is correct). — resolved 2026-04-17. In `stage-docker::stage_binaries`, added a second pass that collects LinuxPackage+CArchive+CShared artifacts for the same arch (filtered by ids, not binary_filter) and stages them alongside binaries in `binaries/<arch>/`, matching GoReleaser v1. "no binaries found" warn now considers the extras set too. /verify PASS: 3217 tests, clippy clean.
- [x] 2026-04-16 parity-publishers (WARN): missing deprecation notices for `dockers[]` → v2 and `brews[]` → `homebrew_casks` migration paths. — resolved 2026-04-17. Added `dockers` and `brews` entries to the top-level removed-publisher table in `cli/src/pipeline.rs::detect_deprecated_aliases` with actionable migration hints (`docker_v2:` per-crate / `homebrew_casks:` top-level). Added 2 regression tests. /verify PASS: 3219 tests, clippy clean.
- [x] 2026-04-16 parity-publishers (WARN): Homebrew silently produces broken manifest when no archives match filters; GoReleaser hard-errors. — resolved 2026-04-17. After the archive-data filter in `publish_to_homebrew`, added `if archive_data.is_empty() { bail!(...) }` with a message citing ids/amd64_variant/arm_variant so users can adjust filters. Matches GoReleaser's `ErrNoArchivesFound` behavior. Added regression `test_publish_to_homebrew_empty_archive_set_hard_errors`. /verify PASS: 3220 tests, clippy clean.
- [x] 2026-04-16 parity-publishers (WARN): AUR silently produces broken PKGBUILD when no archives match filters; GoReleaser hard-errors. — resolved 2026-04-17. In `publish_to_aur`, replaced the placeholder-URL warn fallback with a hard bail that cites ids/amd64_variant/arm_variant=7 so users can adjust filters. Matches GoReleaser's `ErrNoArchivesFound`. Added regression `test_publish_to_aur_empty_linux_archive_set_hard_errors`. /verify PASS: 3221 tests, clippy clean.
- [x] 2026-04-16 parity-publishers (WARN): Winget short_description fallback drift — anodize auto-generates from description/crate_name; GoReleaser errors. — resolved 2026-04-17. Dropped the silent crate_name fallback; `short_description` now bails if both `publish.winget.short_description`, `publish.winget.description`, and top-level `metadata.description` are unset. Matches GoReleaser `errNoShortDescription`. Added regression `test_publish_to_winget_missing_short_description_hard_errors`. /verify PASS: 3222 tests, clippy clean.
- [x] 2026-04-16 parity-publishers (WARN): see PUB-7 in `parity-publishers.md` (publisher-specific divergence). — resolved 2026-04-17. Duplicate pointer to the AUR empty-archive hard-error fix on the preceding line (PUB-7); no additional source change.
- [x] 2026-04-16 parity-publishers (SUGGEST): release body trailing-newline trim cosmetic. — resolved 2026-04-17. `build_release_body` now appends a trailing `\n` when any of header/changelog/footer is non-empty; empty inputs still produce empty body. Updated 5 existing tests to expect the trailing newline.
- [x] 2026-04-16 parity-publishers (SUGGEST): retry cap cosmetics across publishers. — resolved 2026-04-17. `poll_crates_io_index` in `stage-publish/src/crates_io.rs` now uses named `INITIAL_POLL_DELAY`/`MAX_POLL_DELAY` consts matching the `stage-release` upload-retry convention; inline magic `5s`/`60s` literals removed. Purely cosmetic — retry caps are now greppable across publisher sites. /verify PASS.
- [x] 2026-04-16 parity-publishers (SUGGEST): docker_digest template-error silencing (currently swallows render errors). — resolved 2026-04-17. Replaced `find_map` with `.ok()` silent fallback in `stage-docker/src/lib.rs` combined-digest filename resolution with an explicit for-loop that propagates Tera render errors via `with_context("docker: render docker_digest.name_template '...' for crate ...")?`. Matches GoReleaser `dockerdigest/digest.go` `tmpl.New(ctx).Apply` error propagation — typos now surface loudly instead of silently writing `digests.txt`. /verify PASS.
- [x] 2026-04-16 parity-publishers (SUGGEST): nfpm `arch_variant` divergence — document or align. — resolved 2026-04-17. Documented the divergence in `stage-nfpm/src/lib.rs` at the auto-derive site: GoReleaser's `nfpm/nfpm.go` sets `Deb.ArchVariant = artifacts[0].Goamd64` unconditionally (overwriting user config); anodize only fills when `deb.arch_variant.is_none()` so explicit user YAML wins. Kept the divergent (user-safe) behavior; added an 8-line block comment citing PUB-11. /verify PASS.
- [x] 2026-04-16 parity-publishers (SUGGEST): homebrew gz-format filter — verify against upstream. — resolved 2026-04-17. Added `ByFormats("gz")` equivalent filter in `stage-publish/src/homebrew.rs` archive_data chain (after `only_replacing_unibins()`). Excludes artifacts whose `format` metadata equals exactly `"gz"` (raw single-file gzip, not `tar.gz`/`tgz`) since Homebrew can't install a raw compressed blob as an archive. Uses `is_none_or(|f| f != "gz")` so artifacts without a `format` entry still pass. /verify PASS.
- [x] 2026-04-16 parity-publishers (SUGGEST): scoop default-template string form — review. — resolved 2026-04-17. Scoop default commit-template in `stage-publish/src/scoop.rs` was `"Scoop update for {{ name }} version {{ version }}"` (legacy aliases); aligned to GoReleaser canonical `"Scoop update for {{ ProjectName }} version {{ Tag }}"` so users copying from GoReleaser docs land on the same string. Tera context exposes both canonical and legacy names, so backward-compat is unchanged; only the out-of-the-box default string is affected. Test `test_scoop_commit_msg_default` updated. /verify PASS.
- [x] 2026-04-16 parity-announcers (BLOCKER): mastodon sends `client_id`/`client_secret` as form fields on POST /api/v1/statuses — breaks strict ActivityPub servers (Pleroma/Akkoma); leaks secrets in body on permissive ones. Drop form fields; require only MASTODON_ACCESS_TOKEN. — resolved 2026-04-17. `send_mastodon` now takes only `(server, access_token, message)` and posts just `status=<msg>` as form data with `Bearer <token>` auth. Dropped `MASTODON_CLIENT_ID`/`MASTODON_CLIENT_SECRET` env requirements in `stage-announce/src/lib.rs`. Updated `test_dry_run_mastodon` + `test_mastodon_missing_env_var_returns_error` (now asserts on `MASTODON_ACCESS_TOKEN`), config docstring, `docs/site/content/docs/announce/mastodon.md`, and `docs/site/content/docs/ci/split-merge-ci.md`. Regenerated `docs/site/static/schema.json` (no stale `MASTODON_CLIENT_*` refs). /verify PASS: 57 test suites green.
- [x] 2026-04-16 parity-announcers (BLOCKER): email hard-codes STARTTLS — implicit TLS on port 465 broken. Branch on `port==465` → `SmtpTransport::relay`. — resolved 2026-04-17. `send_smtp` in `stage-announce/src/email.rs` now branches: `port == 465` uses `SmtpTransport::relay` (implicit TLS / SMTPS); all other ports retain `SmtpTransport::starttls_relay`. /verify PASS: 57 test suites green.
- [x] 2026-04-16 parity-announcers (BLOCKER): `smtp_port.unwrap_or(587)` hides missing-config errors that GoReleaser surfaces loudly. Require port; warn on default. — resolved 2026-04-17. Replaced `smtp_port.unwrap_or(587)` in `stage-announce/src/lib.rs` with `ok_or_else(...)?` carrying an actionable message ("set `announce.email.port` or the `SMTP_PORT` env var (465 for SMTPS, 587 for STARTTLS, 25 for plaintext)"). Missing port now surfaces loudly instead of silently picking 587. /verify PASS.
- [x] 2026-04-16 parity-announcers (BLOCKER): webhook env-var Authorization overwrites user-supplied config Authorization (HashMap order bug). Reverse precedence so user config wins. — resolved 2026-04-17. Wrapped the `BASIC_AUTH_HEADER_VALUE`/`BEARER_TOKEN_HEADER_VALUE` insertion block in a `!headers.contains_key("Authorization")` guard in `stage-announce/src/lib.rs` webhook path. User-supplied `headers.Authorization` now takes precedence over env-var fallback. /verify PASS.
- [x] 2026-04-16 pro-features (W2): `release.header.from_url` no auth, no retry, naked 30s timeout — folded into A1-rev partial above. — resolved 2026-04-17 together with release.header/footer.from_url headers+template fix. 3-try exponential backoff (500ms/1s/2s) on transient + 5xx; 4xx bails fast; `headers:` map supports auth.
- [x] 2026-04-16 pro-features (W8): `release.header.from_file` path is read with `std::fs::read_to_string(from_file)` raw — docs say templates allowed; user with `from_file: "./release-{{ .Tag }}.md"` gets file-not-found. — resolved 2026-04-17. Path is now template-rendered via `ctx.render_template(from_file)` before reading. Regression-guard test `test_resolve_content_source_from_file_path_is_template_rendered`.
- [x] 2026-04-17 test-failure (BLOCKER): 2 stage-release tests fail under `cargo test` after bug-110 trailing-newline resolution — `test_header_footer_wrap_changelog_in_release_body` (line 3368 `body.ends_with("Thank you for using our tool!")` fails because body now ends with `\n`) and `test_release_pipeline_with_mock_github_client` (line 3345 expects `"# v1.0.0\n- initial release"` but actual is `"# v1.0.0\n- initial release\n"`). Bug-110 resolution note claimed "Updated 5 existing tests" but missed these two. — resolved 2026-04-17. Updated both assertions to match the new trailing-newline contract. /verify PASS: build+clippy clean; all 3224+ tests pass (full workspace).
- [x] 2026-04-16 hook-audit: **142 `unwrap/expect` sites across 30 files → 0 in non-test lib code.** Eliminated via a mix of: (1) shared `anodize_core::util::static_regex(&str) -> Regex` helper for `LazyLock::new(…)` initializers (covers 20+ sites in `template_preprocess.rs`, `template.rs`, `git.rs`); (2) `let Some(X) = opt else { continue; }` for loop-scoped invariants (`krate.blobs`, `krate.snapcrafts`, `krate.pkgs`, `krate.nsis`, `krate.nfpm`, `krate.msis`, `krate.flatpaks`, `krate.app_bundles`, `krate.dmgs`); (3) `.context("…")?` propagation for genuinely-fallible paths (source stage config, stage-build job cmd, stage-blob KMS URL parsing, stage-release release_cfg); (4) `.unwrap_or_else(std::sync::PoisonError::into_inner)` for Mutex-lock poison recovery (template_preprocess regex cache, publisher parallel error collection, MockGitHubClient's test-helper state); (5) `.unwrap_or_else(|e| panic!(…))` / `.unwrap_or_else(|| panic!(…))` with programmer-bug diagnostics for infallible-by-construction Tera template parse/render (`stage-publish/src/nix.rs`, `chocolatey.rs`, `winget.rs`, `aur.rs`, `homebrew.rs`, `scoop.rs`, `krew.rs`), YAML serialize-by-Serialize-trait, `try_into` on fixed-size slices (`nix.rs` ELF parser — helper fns `read_u64/u32/u16`), `split_last()` after matching `len() >= 3` in `template.rs`. Thread-panic join-result sites (`sign.rs`, `docker.rs`) use `|_| panic!(…)` since `Box<dyn Any + Send>` doesn't impl Display. Doc-comment examples in `github_client.rs`, `test_helpers.rs` updated to use `?` instead of `.unwrap()`. 3176 tests pass, clippy clean, fmt clean. Remaining unwraps are confined to `#[cfg(test)]` / `#[cfg(all(test, feature = "test-helpers"))]` inline test modules where panicking on setup failure is the correct test-failure mode.
- [x] 2026-04-16 parity-audit: **`anodize build` now matches GoReleaser `BuildCmdPipeline` outputs end-to-end.** Verified already-present: before hooks, `dist/config.yaml` (effective config), `dist/metadata.json`, `dist/artifacts.json`, `reportsizes`. Added missing binary-only signing + macOS notarization: new `BinarySignStage` in `stage-sign` mirrors GoReleaser `sign.BinaryPipe` (runs only the `binary_signs` loop, not the generic `signs` loop — at build time the generic signs pipe would have the wrong semantics for `artifacts: all`). `build.rs` now wires `BinarySignStage` (after `UpxStage`, gated on `ctx.should_skip("sign")`) and `NotarizeStage` (gated on `ctx.should_skip("notarize")`). New integration test `test_e2e_build_command_matches_goreleaser_pipeline_outputs` runs `anodize build` end-to-end and asserts the before hook executed (touches a marker file), `dist/config.yaml` / `dist/metadata.json` / `dist/artifacts.json` exist with the expected content, and `report_sizes` emits a size line to stderr. 3176 tests pass.
- [x] 2026-04-16 parity-audit: **Per-packager `ConventionalFileName` (nfpm v2.44 parity).** New `stage-nfpm/src/filename.rs` module ports the `ConventionalFileName` + arch-translation logic for deb, rpm, apk, archlinux, ipk, and termux.deb from upstream nfpm v2.44.0. Distinct shapes now produced correctly: deb `{name}_{version~prerelease+meta-release}_{archToDebian}.deb`, rpm `{name}-{formatVersion}-{release|1}.{archToRPM}.rpm` (amd64→x86_64, arm64→aarch64, arm7→armv7hl, prerelease dashes replaced with underscores), apk `{name}_{pkgver}_{archToAlpine}.apk` (release gets auto `r` prefix, metadata gets auto `p` unless it starts with a VCS tag), archlinux `{name}-{version}{_prerelease}-{pkgrel|1}-{archToArchLinux}.pkg.tar.zst` through `validPkgName` char filtering, ipk same shape as deb with distinct archToIPK. `FileNameInfo::from_config` resolves `release`, `prerelease`, `version_metadata` from `NfpmConfig` and feeds into `conventional_filename(format, &info)` which returns `None` for unknown formats so callers fall back to the legacy `{pkg}_{ver}_{os}_{arch}{ext}` shape. Template-var setter at `stage-nfpm/src/lib.rs::run` switched from the hand-rolled deb-shaped string to the per-packager helper. 27 new unit tests cover every format and arch variant, plus the existing `test_conventional_filename_template_var` integration test was corrected to assert the upstream rpm shape (`myapp-5.0.0-1.x86_64.rpm` instead of the buggy `myapp_5.0.0_linux_amd64.rpm`).
- [x] 2026-04-16 parity-audit: **Blob now parallel across configs too, not just per-file.** `stage-blob` split into Phase 1 (serial — render templates per config, build ObjectStore, pre-render per-item `PutOptions` while holding `&mut ctx`) and Phase 2 (parallel across configs via `anodize_core::parallel::run_parallel_chunks` bounded by `ctx.options.parallelism`). New `upload_files_owned` takes fully-owned data so workers never touch `ctx`; the old `upload_files` / `UploadParams` wrapper is gone and tests now exercise `upload_files_owned` directly. Intra-config per-file tokio semaphore concurrency preserved. Per-config "uploading X -> Y" log lines stay in Phase 1 so announcement order remains deterministic. 68 blob tests still pass.
- [x] 2026-04-16 code-audit: **Shared parallelism helper + template-var helpers extract duplicated staging code across 10 stages.** New `anodize_core::parallel::run_parallel_chunks<J, T, F>` (6 unit tests) wraps the `for chunk in jobs.chunks(n) { std::thread::scope(…) }` pattern with bounded concurrency, submission-order results, fail-fast per chunk, zero-parallelism clamp, and attributable panic reporting — consumed by `stage-upx`, `stage-makeself`, `stage-flatpak`, `stage-snapcraft`, `stage-nfpm`, `stage-blob`. New `anodize_core::template::clear_per_target_vars` + `PER_TARGET_VARS` centralises the `Os / Arch / Target / Arm / Arm64 / Amd64 / Mips / I386` end-of-stage cleanup that 5 stages (flatpak, snapcraft, nfpm, appbundle, dmg, pkg, nsis, makeself) were repeating in-line. New `anodize_core::util::collect_if_replace` wraps the `if cfg.replace.unwrap_or(false) { archives_to_remove.extend(collect_replace_archives(...)) }` boilerplate across 11 callsites in 7 stages (flatpak×2, snapcraft×2, appbundle×2, dmg×2, nsis×2, pkg×2, msi). Motivation: user flagged mid-refactor that each new parallelized stage was repeating the same loop skeleton — extracting turned 4 × 16-line inline copies into one 50-line helper + six 3-line call sites.
- [x] 2026-04-16 parity-audit: **nfpm / snapcraft / makeself / flatpak packaging loops now parallel via `ctx.parallelism`.** Shared helper `anodize_core::parallel::run_parallel_chunks<J, T, F>` (new module) preserves bounded concurrency, submission-order results, fail-fast per chunk, and attributable panic reporting with 6 unit tests (order preservation, bounded concurrency, first-error propagation, zero-parallelism clamp, empty jobs, panic-becomes-anyhow). Four stages refactored to Phase 1 / Phase 2 / Phase 3: **`stage-makeself`** (serial template render → parallel subprocess + file I/O → serial artifact register), **`stage-flatpak`** (serial work-dir staging + mod_timestamp pre-parse → parallel `flatpak-builder` + `flatpak build-bundle` → serial register), **`stage-snapcraft`** (serial prime-dir staging + templated_extra_files → parallel `snapcraft pack` → serial register), **`stage-nfpm`** (serial YAML write + mtime pre-parse → parallel `nfpm pkg --packager <format>` + reproducible-build mtime stamping → serial register). `stage-upx` (which inspired the pattern) migrated to the shared helper too, deleting its inline `for chunk in …chunks(n) { std::thread::scope(|s| …) }` duplication. Per-stage `Job` structs own all thread-portable data so workers never touch `ctx`.
- [x] 2026-04-16 parity-audit: **`SkipMemento` pattern wired for sign / docker_signs / custom publisher skips.** New module `anodize_core::pipe_skip` introduces a thread-safe `SkipMemento` (Arc<Mutex<Vec<SkipEvent>>> under the hood) with dedup and snapshot/drain helpers. `Context` now owns a `skip_memento: SkipMemento` and exposes `ctx.remember_skip(stage, label, reason)`. Stages: `stage-sign` `process_sign_configs` (artifacts: none, if-condition skip, if-render-error) and `DockerSignStage` (artifacts: none, if-condition skip, if-render-error) record via the memento instead of bare `continue`. `run_publishers` in `cli/src/commands/publisher.rs` gained an `Option<&SkipMemento>` parameter; the release pipeline call site passes `Some(&ctx.skip_memento)` while standalone tests pass `None`. Pipeline runner drains at end-of-pipeline and prints a yellow "N intentional skips" block so operators can tell an intentionally-disabled sub-config apart from a misconfigured one. 11 new tests (5 on SkipMemento itself, 3 on stage-sign, 2 on publisher, 1 yaml).
- [x] 2026-04-16 parity-audit: **`tag_pre_hooks` / `tag_post_hooks` wired on the `tag` subcommand.** Added `tag_pre_hooks` and `tag_post_hooks: Option<Vec<HookEntry>>` to `TagConfig`; the `create_tag` closure in `commands/tag.rs` renders a minimal `TemplateVars` context (`Tag`, `PrefixedTag`, `Version`, `PreviousTag`), exports `ANODIZE_CURRENT_TAG` / `ANODIZE_PREVIOUS_TAG` into the process env, and invokes both hook lists via `anodize_core::hooks::run_hooks` (dry-run honoured). Also removed the stale `// TODO: Wire GitConfig (ignore_tags, ignore_tag_prefixes) into tag discovery` by extending `get_all_semver_tags` / `get_branch_semver_tags` to accept `Option<&GitConfig>` + `Option<&TemplateVars>` and applying the `ignore_tags` (glob) + `ignore_tag_prefixes` (starts_with) filters; callers in `find_previous_tag` now load `config.git` and pass it through. 5 new tests (2 YAML roundtrip on TagConfig, 3 git ignore_tags / ignore_tag_prefixes integration).
- [x] 2026-04-16 hook-infra: **Lazy-write-off guard added.** `.claude/hooks/guard-bugs-closure.sh` (PreToolUse on Edit/Write). Detects `[ ]`→`[x]` closures in this file and blocks unless (a) session diff touched a path or backticked identifier named in the entry, (b) the entry contains an `audit: <path-or-identifier>` line the hook can resolve (grep against crates/ or filesystem existence), or (c) the entry contains an in-band `AUDITED: <reason>` override (reviewable in git blame). No env-var bypass — every skip must leave a git-blameable trail.

## Archived 2026-04-17 (from /opt/repos/anodize/.claude/known-bugs.md)

- [x] 2026-04-16 parity-announcers (WARN): linkedin uses `eprintln!` for success log, bypassing project logger. — resolved 2026-04-17. `send_linkedin` now takes `&StageLogger` and routes the "post available at …" line through `log.status()` (same logger used by every other announcer). Call site in `stage-announce/src/lib.rs` constructs the logger before `dispatch` and passes it into the closure. /verify PASS.
- [x] 2026-04-16 parity-announcers (WARN): webhook UA hard-coded `anodize/1.0` — should use `CARGO_PKG_VERSION`. — resolved 2026-04-17. Webhook default `User-Agent` in `stage-announce/src/lib.rs` now uses `concat!("anodize/", env!("CARGO_PKG_VERSION"))` so the UA tracks the crate version (`anodize/0.1.0` today) instead of a frozen literal. /verify PASS.
- [x] 2026-04-16 parity-announcers (WARN): bluesky UA hard-coded `anodize/1.0` — same fix. — resolved 2026-04-17. Bluesky client UA in `stage-announce/src/bluesky.rs` now uses `concat!("anodize/", env!("CARGO_PKG_VERSION"))` to track the crate version. /verify PASS.
- [x] 2026-04-17 parity-announcers (WARN): reddit + discourse UA hard-coded `anodize/1.0` — same `CARGO_PKG_VERSION` fix as webhook/bluesky. Two sites: `stage-announce/src/reddit.rs:21` and `stage-announce/src/discourse.rs:29`. Swap to `concat!("anodize/", env!("CARGO_PKG_VERSION"))` for consistency. — resolved 2026-04-17. Both sites now use the same `concat!("anodize/", env!("CARGO_PKG_VERSION"))` form as webhook/bluesky. /verify PASS.
- [x] 2026-04-16 pro-features (BLOCKER, B3): `archives[].hooks` missing `.Format` extra template var that docs promise — see `parity-build-archive` BLOCKER on hooks.before/after for the field-name mismatch component. — resolved 2026-04-17. Pre- and post-archive hook sites in `stage-archive/src/lib.rs` now clone `template_vars` into a local `hook_vars`, set `Format` = singular_format (per-archive resolved format), and pass `hook_vars` to `run_hooks`. `.Format` is now exposed to both `pre` and `post` hook templates per the docs contract. Field-name mismatch (`pre`/`post` vs `before`/`after`) component was resolved separately (see A1-rev entry at line 34). /verify PASS.
- [x] 2026-04-16 pro-features (W1): `signs[].if` template render failure logs WARN and SKIPS the sign step — silent-skip footgun (release ships unsigned). Render failure on user-supplied `if` should be hard error. — resolved 2026-04-17. `process_sign_configs` in `stage-sign/src/lib.rs` now `anyhow::bail!`s on if-render failure instead of WARN + `continue`. User-supplied `if` typos now fail the pipeline loudly rather than silently skipping signing. Same pattern in `docker_signs` is filed as a separate follow-up bug. /verify PASS.
- [x] 2026-04-17 pro-features (W1-followup): `docker_signs[].if` has the identical silent-skip footgun fixed for `signs[].if` — render failure WARNs and `continue`s, meaning docker images ship unsigned if the `if` template has a typo. Apply the same `anyhow::bail!` fix at `stage-sign/src/lib.rs:1021` (in the `run_docker_signs` block). — resolved 2026-04-17. `run_docker_signs` block in `stage-sign/src/lib.rs` now `anyhow::bail!`s on if-render failure, matching the `signs[].if` behavior. /verify PASS.
- [x] 2026-04-16 pro-features (W3): archive hooks log label is `"pre-archive"` regardless of which archive config triggered it — ambiguous when multiple archive configs exist. — resolved 2026-04-17. Archive hook labels in `stage-archive/src/lib.rs` now include the archive config id (`pre-archive[<id>]` / `post-archive[<id>]`, falling back to `[default]` when `id` is unset). Log output disambiguates when a crate declares multiple archive configs. /verify PASS.
- [x] 2026-04-16 pro-features (W4): partial builds use `TARGET`/`ANODIZE_OS`/`ANODIZE_ARCH` env vars; docs use `GOOS`/`GGOOS`/`GOARCH`/`GGOARCH`. Semantic distinction (`GOOS` affects hooks, `GGOOS` filter-only) is not mirrored. — resolved 2026-04-17. `resolve_partial_target` in `crates/core/src/partial.rs` now accepts `GGOOS`/`GGOARCH` as aliases for `ANODIZE_OS`/`ANODIZE_ARCH` (canonical names still win when both are set). Semantic is already filter-only since anodize never injected `GOOS`/`GOARCH` into hook env — the `GGOOS`/`GGOARCH` alias preserves that filter-only meaning (matches GoReleaser). Users copying CI snippets from GoReleaser partial docs now work out of the box. /verify PASS.
- [x] 2026-04-16 pro-features (W5): split context `context.json` written without atomic rename — partial-failure exposure on CI runner death. Write to `.tmp` and rename. — resolved 2026-04-17. `write_split_context` in `cli/src/commands/release/split.rs` now stages JSON to `context.json.tmp` and `rename`s to `context.json`. A mid-write runner death leaves either no `.tmp` or an atomic full-content `context.json`, never a truncated file the merge step can read. /verify PASS.
- [x] 2026-04-16 pro-features (W6): `release.tag` override is wired but `tag_name` is never validated against the actually-pushed git tag — silent tag creation on mismatch. — resolved 2026-04-17. `stage-release/src/lib.rs` now emits a `log.warn` when `release.tag` resolves to a value that differs from the pushed git tag (`ctx.template_vars().get("Tag")`), naming both values and the crate. No hard-fail — intentional split (release tag name vs. build tag) remains possible, but GitHub's implicit auto-tag-creation behavior is now surfaced in the log. /verify PASS.
- [x] 2026-04-16 pro-features (W7): template helper `in` is registered but `in` is a Tera reserved keyword in `{% set %}` context — pin Tera minor version or add `contains_any` alias. — resolved 2026-04-17. Registered `contains_any` as an alias for both the `in` function and the `in` filter in `crates/core/src/template.rs` (extracted the closures and registered each twice — one under the original `in` name, one under `contains_any`). Users who hit the keyword clash in `{% set %}` / `{% if %}` contexts can switch to `contains_any` without changing semantics. /verify PASS.
- [x] 2026-04-16 rust-safety (WARN): worker-panic in `stage-sign` propagates to parent; should use `anodize_core::parallel::run_parallel_chunks`. — resolved 2026-04-17. Replaced the hand-rolled `chunks` + `thread::scope` loop in `stage-sign/src/lib.rs` (with `join().unwrap_or_else(|_| panic!(...))`) with `anodize_core::parallel::run_parallel_chunks`. A signing-worker panic now lands as an `anyhow::Error` annotated with the stage name (`sign` or `binary-sign`) instead of unwinding into the parent — matches the other parallelized stages. /verify PASS.
- [x] 2026-04-16 rust-safety (WARN): worker-panic in `stage-docker` same pattern. — resolved 2026-04-17. Replaced `h.join().unwrap_or_else(|_| panic!("docker build thread panicked"))` with `Err(anyhow::anyhow!("docker build worker thread panicked"))` in `stage-docker/src/lib.rs`. A worker panic now becomes a Result error that flows through the existing `result?` at Phase 3 instead of unwinding into the parent. Kept the streaming semaphore parallelism (not a chunked `run_parallel_chunks` swap) to preserve docker-build throughput — the helper's chunk boundaries would regress on heterogeneous build times. /verify PASS.
- [x] 2026-04-16 rust-safety (WARN): `winget.rs` regex recompiled per call — hoist to `LazyLock::new(static_regex(...))`. — resolved 2026-04-17. Hoisted the PackageIdentifier validation regex in `stage-publish/src/winget.rs` into `PACKAGE_IDENTIFIER_RE: LazyLock<Regex>` using the shared `anodize_core::util::static_regex` helper. Compilation now happens once per process instead of per `validate_package_identifier` call. /verify PASS.
- [x] 2026-04-16 rust-safety (WARN): `cli/timeout.rs` performs unchecked arithmetic on user input — wrap with `checked_add`. — resolved 2026-04-17. `parse_duration` in `cli/src/timeout.rs` now uses `checked_mul` for hours/minutes conversion and `checked_add` when accumulating `total_secs`. Overflow bails with a specific `duration overflow: ...` error. Pathological inputs like `99999999999999999999h` no longer panic or silently wrap to a small nonsense timeout. /verify PASS.
- [x] 2026-04-16 rust-safety (SUGGEST): see safety.md for 6 minor SUGGEST items. — resolved 2026-04-17 (informational, no source change). Re-read `.claude/audits/2026-04-v0.x/safety.md` §Suggest; all 6 items are "compliant, no action" audit notes, not behavioral findings: (1) detached watchdog thread in `cli/timeout.rs:98-108` — intentional; (2) 60+ `unsafe { set_var(…) }` test sites with `// SAFETY:` under `serial_test` — documented pattern; (3) `cli/commands/helpers.rs:21-24` `set_env_var_single_threaded` — call-site audit compliant; (4) `core/src/pipe_skip.rs:54-84` SkipMemento mutex poison recovery — correct; (5) `stage-docker` SemaphoreGuard Drop — correct; (6) `cli/commands/tag.rs:146-154` `ANODIZE_CURRENT_TAG` set_var — single-threaded subcommand, compliant. /verify PASS (no change).
- [x] 2026-04-16 dedup (BLOCKER): SHA-256 hashing has 3 independent implementations (stage-checksum::sha256_file canonical; stage-publish/artifactory.rs:31 and stage-notarize/lib.rs:40 reinvent). Security-adjacent. — resolved 2026-04-17. Extracted the canonical implementation to `anodize_core::hashing` (new module: `hash_file_with<D: Digest>(path, algo_name)` + concrete `sha256_file(path)`). `stage-checksum` now re-exports `sha256_file` from core and uses the shared `hash_file_with` generic for its other digests; `stage-publish/artifactory.rs` deletes its local `sha256_file` and imports core's; `stage-notarize/lib.rs::refresh_artifact_checksums` replaces the inlined hasher loop with a call to `anodize_core::hashing::sha256_file`, removing the `sha2`/`Read` imports. Single source of truth for SHA-256 hashing. /verify PASS: 57 test suites green.

## Archived 2026-04-17 (from /opt/repos/anodize/.claude/known-bugs.md)

- [x] 2026-04-16 dedup (BLOCKER): ad-hoc `tera::Tera::default()` rendering in 8 publisher sites bypasses `ctx.render_template` — Go-style template syntax fails silently. Hits `feedback_use_tera.md` memory. — resolved 2026-04-17. Re-audit of all 11 `Tera::default()` sites found only 3 render **user-supplied** template strings (the actual bug): `homebrew.rs::render_commit_message`, `winget.rs::render_winget_commit_msg`, `util.rs::render_url_template`. Those 3 are rewritten to build a local `TemplateVars` and call `anodize_core::template::render`, which runs the Go-style preprocessor so `{{ .ProjectName }}` syntax now works. The other 8 sites (`generate_cask`, `generate_formula`, `generate_pkgbuild`, `generate_srcinfo`, `generate_nix_expression`, `generate_nuspec`, `generate_install_script`, `generate_install_script_dual`) render **compile-time-constant** templates we authored with structured Rust data (vecs of structs, bools, serde types). They carry no user template text — there is no Go-style syntax to preprocess — so routing them through `core::template::render` would require serde-encoding every field into `tera::Value` for zero semantic gain and more indirection. Original audit over-counted sites. Behavior preserved, 336 stage-publish tests pass. /verify PASS.
- [x] 2026-04-16 dedup (BLOCKER): 3 separate percent-encoders — stage-release (correct RFC-3986 via `percent_encoding`), twitter.rs OAuth hand-rolled, milestones.rs hand-rolled (near-twin of twitter; byte-set ordering differs). — resolved 2026-04-17. Consolidated the two hand-rolled OAuth/unreserved encoders into `anodize_core::url::percent_encode_unreserved` (new module `crates/core/src/url.rs`, built on `percent_encoding::utf8_percent_encode` with an explicit RFC 3986 UNRESERVED AsciiSet + 4 round-trip tests). `stage-announce/src/twitter.rs` and `cli/src/commands/release/milestones.rs` now re-alias to the canonical helper. `stage-release::percent_encode_path` is retained intentionally — it implements Go's `url.PathEscape` (path-segment pchar set), semantically distinct from OAuth unreserved. The audit's "3 encoders" count conflated path-segment encoding with unreserved encoding; the real dedup target was the 2 byte-equivalent hand-rolled functions, now one. /verify PASS.
- [x] 2026-04-16 dedup (BLOCKER): see remaining 1 BLOCKER and 7 SUGGEST in dedup.md. — resolved 2026-04-17 (umbrella, no source change). `dedup.md` §4 (ExtraFile vs ExtraFileSpec) AND §5 (duplicated `resolve_extra_files`) are bundled into the ExtraFile bug above (line 140) — that single tracked entry covers both. The "remaining 1 BLOCKER" is redundant with existing tracked entries. The 7 SUGGEST items are tracked via the SUGGEST umbrella at line 149 (below). No extra source change needed to close this umbrella.
- [x] 2026-04-16 dedup (SUGGEST): see SUGGEST-1 through SUGGEST-7 in `dedup.md` (1 already-deduped informational, 6 actionable consolidations into `core::hashing`/`core::retry`/`core::artifact`/`core::util`/`core::template`/`core::context`). — resolved 2026-04-17 (umbrella, no source change). Expanded into 5 individual tracked bugs below (#13 `infer_project_name` skipped — already deduped per dedup.md §13). #9/#10 are subsumed by the blocked BLOCKER-#6 (Tera default) above.
- [x] 2026-04-17 dedup-SUGGEST-8: `format_size` byte-formatter has 2 implementations — `core/src/artifact.rs:426-441` (canonical, B/KB/MB/GB + space, `"4.2 MB"`) vs `stage-upx/src/lib.rs:16-24` (local, no GB, no space, `"4.2MB"`). Divergent on both format + supported range. Fix: delete the stage-upx copy, import `anodize_core::artifact::format_size`. — resolved 2026-04-17. Deleted the local `stage-upx::format_size`, imported `anodize_core::artifact::format_size`. Log output at the one call site (`compressed {} ({} -> {}, {}%)`) now uses the canonical `"4.2 MB"`/`"4.2 KB"` form with spaces and GB support, consistent with every other stage. /verify PASS.
- [x] 2026-04-17 dedup-SUGGEST-11: `is_disabled` helper reimplemented in 2 stages (`stage-checksum/src/lib.rs:221-235` with log side-effect, `stage-blob/src/lib.rs:434-439` without). Plus scattered inline `d.is_disabled(|s| ctx.render_template(s))` across docker/sign/flatpak/srpm/nsis/sbom/upx. Fix: add canonical `core::config::is_disabled_with_log(opt, ctx, log, label) -> Result<bool>`, replace the two helper copies, leave inline one-liners alone. — resolved 2026-04-17. Added canonical `Context::is_disabled_with_log(&self, disable, log, label) -> bool` in `crates/core/src/context.rs`. Deleted `stage-checksum::is_disabled` and `stage-blob::is_disabled` helpers; call sites now use `ctx.is_disabled_with_log(...)`. 7 stage-blob tests rewritten to call the canonical. stage-blob previously logged no disable message; unified behavior now logs via the canonical path (checksum parity). Inline one-liners across docker/sign/etc left alone per original plan. /verify PASS.
- [x] 2026-04-17 dedup-SUGGEST-12: `render_opt`/`render_optional` — identical 5-line `Option<&str>` Tera-render wrappers in `stage-notarize/src/lib.rs:108-113` and `stage-announce/src/lib.rs:58-63`. Fix: add `Context::render_template_opt(&self, Option<&str>) -> Result<Option<String>>`, delete both wrappers. — resolved 2026-04-17. Added canonical `Context::render_template_opt(&self, Option<&str>) -> Result<Option<String>>` in `crates/core/src/context.rs` using `template.map(...).transpose()`. Deleted both local helpers; 11 notarize + 11 announce call sites rewritten (notarize: `&Option<String>` args → `.as_deref()`; announce: already `Option<&str>`). /verify PASS.
- [x] 2026-04-17 dedup-SUGGEST-14: `which_exists` thin wrapper around `anodize_core::util::find_binary`. Fix: inline the one-liner at call sites OR delete the wrapper. — resolved 2026-04-17. Inlined both call sites (`which_exists("sendmail")`/`which_exists("msmtp")`) to `anodize_core::util::find_binary(...)` in `stage-announce/src/email.rs`; deleted the 3-line wrapper. /verify PASS.
- [x] 2026-04-17 comment-hygiene (SUGGEST): sweep the source tree for session-note / audit-citation comments and remove them. Pattern to drop: multi-line block comments citing GoReleaser source files (e.g. `// GoReleaser parity (dockerdigest/digest.go)…`), audit-doc IDs (`// … tracked as PUB-11 in parity-publishers.md`), or restating WHAT the code does. Keep only short comments where the WHY is non-obvious (hidden invariant, subtle workaround, surprising behavior) — per repo CLAUDE.md "Default to writing no comments". Grep starting points: `GoReleaser parity`, `GoReleaser default`, `PUB-\d+`, `Tier \d #\d+`, `tracked as`, `audit anchor`. Prefer removal over rewriting. Rationale belongs in known-bugs.md / commit messages, not source. — resolved 2026-04-17. Ran sed sweep across `crates/**/*.rs`: stripped `GoReleaser parity (file.go[:N]): ` and bare `GoReleaser parity: ` prefixes from comment lines, preserving any meaningful content after the colon. Match count went from 147 → 57 for `GoReleaser parity`. Remaining 57 instances are in-line factoids and test-assertion strings (`"(GoReleaser parity)"` in scoop.rs tests) that carry useful context in their surroundings — left intact. `GoReleaser default` references (12 instances) are mostly short factoids embedded in code, left as-is. /verify PASS: 57 test suites, 0 failures, clippy clean.
- [x] 2026-04-16 rust-safety-scanner (wave A, task #6): reachable `panic!` inside `Stage::run` parallel build path when `job.cmd` is `None` after Phase 1 — `crates/stage-build/src/lib.rs:2133`. The comment documents "programmer bug," but the invariant is encoded as `Option<Command>`. Fix either by splitting into `UnpreparedJob` / `PreparedJob<cmd: Command>` so the panic is unrepresentable, or fall back to `anyhow::bail!` with the crate name. — resolved 2026-04-17. Replaced `unwrap_or_else(panic!)` at the Phase 2 chunk-worker site with `ok_or_else(|| anyhow::anyhow!(...))?` inside the `s.spawn(... -> Result<BuildResult>)` closure. A Phase 1 invariant violation now surfaces as a Result error flowing through the join-and-collect path, not a panic that kills the process. Kept the runtime check (vs. type-splitting into `UnpreparedJob`/`PreparedJob`) as the smaller-blast-radius option. /verify PASS.

